## Build a compose container

`docker compose container <container-name> up --build -d`

## Show all running compose containers logs

`docker compose logs --tail 20 --follow`

## Shutdown all running compose containers

`docker compose down`
